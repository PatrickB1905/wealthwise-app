# =============================================================================
# Analytics Service — LOCAL / NON-DOCKER environment
# =============================================================================
# Copy to:
#   services/analytics/.env
#
# Run locally from services/analytics:
#   python -m uvicorn main:app --reload --port %ANALYTICS_SERVICE_PORT%
#
# PowerShell example:
#   $env:ANALYTICS_SERVICE_PORT=7000; python -m uvicorn main:app --reload --port $env:ANALYTICS_SERVICE_PORT
#
# Notes:
# - LOCAL runs use localhost ports.
# - DOCKER runs use the root .env (repo root) and Docker DNS names (e.g. db, market-data).
# =============================================================================

# Local Postgres running on your machine (not inside Docker)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/wealthwise

# Market Data service running locally on your machine
MARKET_DATA_SERVICE_URL=http://localhost:5000/api

# Positions service running locally on your machine
POSITIONS_SERVICE_URL=http://positions:4000/api

# Browser origin for CORS (where the frontend is opened)
FRONTEND_ORIGIN=http://localhost:5173

# Port this service listens on locally
ANALYTICS_SERVICE_PORT=7000

# Logging level: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO

# Analytics cache for expensive calculations (yfinance calls)
ANALYTICS_CACHE_TTL_SECONDS=30
# Maximum lookback for performance/risk endpoints
ANALYTICS_MAX_DAYS=730